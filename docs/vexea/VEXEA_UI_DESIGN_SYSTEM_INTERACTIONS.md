# VEXEA UI Design-System Interactions

**Consolidated:** This former document has been merged into `VEXEA_UI_DESIGN_SYSTEM.md`.

The authoritative source now contains the shared CSS file path, motion tokens, icon states, one measured underline primitive for every tab row, settings controls, quiet ambient categories, reduced-motion behavior, state attributes, and the thin vanilla TypeScript binding API. Do not maintain separate settings-tab, navigation-tab, icon, or ambient motion recipes here.

Use `VEXEA_UI_ANIMATION_CONTRACT.md` for surface ownership and accepted motion-category placement. Use `VEXEA_UI_LOADING_BATTLEPASS_CONTRACT.md` for the accepted loading-bar and full-season battle-pass geometry and runtime semantics.
